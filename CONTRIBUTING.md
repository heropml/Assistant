# 贡献指南

## 开始之前

请先查看现有 Issues。较大的功能或交互调整建议先提交功能建议，说明使用场景与预期行为。

## 开发流程

1. 从最新 `main` 创建分支，保持每次修改聚焦一个问题。
2. 使用支持 Swift 6 的 Xcode，按照 README 构建和启用 Finder 扩展。
3. 涉及行为变化时补充必要的回归测试，运行 `./Scripts/test-core.sh`。
4. 涉及界面时检查中英文布局；涉及 Finder 时验证文件、文件夹和空白处菜单。
5. 提交前运行 `git diff --check`，不要提交 `dist/`、构建缓存或本机配置。

## 提交格式

使用 Conventional Commits 类型及简洁中文标题，例如：

```text
fix: 修复剪切任务完成后覆盖新批次的问题

1. 修复内容

- 描述触发条件和修复后的行为

2. 验证

- 记录实际运行的测试及结果
```

常用类型：`feat`（功能）、`fix`（修复）、`docs`（文档）、`refactor`（重构）、`test`（测试）、`chore`（维护）。不要在未运行测试时声称通过。

## Pull Request

请填写仓库 PR 模板，说明问题、最终行为、验证结果和相关 Issue。界面修改可附中英文截图；配置格式变化须说明兼容与迁移方式。

## 问题反馈

请提供 macOS 版本、芯片架构、应用版本、复现步骤及预期与实际结果。日志和截图中请移除私人文件路径、凭据及其他敏感内容。
